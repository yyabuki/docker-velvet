default: velveth ${DIR} 31 -fastq.gz -shortPaired ${READS} && velvetg ${DIR} -cov_cutoff auto
careful: velveth ${DIR} 91 -fastq.gz -shortPaired ${READS} && velvetg ${DIR} -cov_cutoff 10
adaptive: adaptive ${READS} ${DIR}
version: velveth
